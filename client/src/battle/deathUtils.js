import { tags } from "../helpers/cards"
import { updateTypeStats } from "./boardUtils"

export const deathEffect = ({
  creature,
  hand,
  setHand,
  updateHandCard,
  board,
  setBoard,
}) => {
  return
}