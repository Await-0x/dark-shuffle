import { addCreatureAttribute, updateCreatureStats } from "./boardUtils";
import { updateCardIdCost } from "./handUtils";

export const adventurerSelfDamagedEffect = ({
  damage,
  board,
  setBoard,
  hand,
  damageMonster,
  setHand,
  increaseEnergy,
}) => {
  return
}

export const adventurerHealedEffect = ({
  heal,
  board,
  setBoard,
  damageMonster,
}) => {
  return
}